

Pod::Spec.new do |s|
 

  s.name         = "WSTextField"
  s.version      = "1.0.0"
  s.summary      = "A Library for iOS to use for WSTextField."

  s.homepage     = "https://github.com/Zws-China/WSTextField"

  s.platform     = :ios

  s.license      = "MIT"


  s.author             = { "784846767@qq.com" => "784846767@qq.com" }

  s.source       = { :git => "https://github.com/Zws-China/WSTextField.git", :tag => "#{s.version}" }

  s.source_files  = "WSTextField/*.{h,m}"



end
